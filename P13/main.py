def encode_direct(receive_list):
    object_amount = 0
    Progression_split = 0
    for reference in receive_list:
        if receive_list.index(reference) == 0 or reference == target:
            object_amount+=1
        else:
            receive_list.append
            del receive_list[Progress_split:target]
